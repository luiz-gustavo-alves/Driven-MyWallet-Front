import styled from "styled-components";

const ReturnButton = styled.button`

    width: 26px;
    height: 26px;
    background: inherit;
    border: none;
    cursor: pointer;

    &:hover {
        opacity: 0.7;
    }

    img {
        width: 100%;
    }
`;

export default ReturnButton;